<odoo><data>
    <record id="partner_is_lecturer_view" model="ir.ui.view">
        <field name="name">res.partner.view.lecturers</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>

        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="is_lecturer" invisible="is_company"/>
            </xpath>
        </field>
    </record>

    <record id="partner_is_lecturer_filter" model="ir.ui.view">
        <field name="name">res.partner.select.inherit</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter" />
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='type_company']" position="after">
                <filter string="Lecturer" name="lecturer_filter" domain="[('is_lecturer', '=', True)]" />
            </xpath>
        </field>
    </record>
</data></odoo>